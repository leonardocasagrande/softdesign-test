import styled from 'styled-components';

export const Container = styled.div`
  background-color: rgb(50, 50, 50);
  border-radius: 4px;
  padding: 8px 16px;
  display: flex;
  gap: 16px;
  align-items: center;
  position: fixed;
  bottom: 16px;
  left: 16px;
  color: var(--white);
`;
